import {
  FETCH_INIT,
  FETCH_SUCCESS,
  FETCH_FAIL
} from '../constants/actionTypes';

const dataFetchReducer = (state, action) => {
  const { type, payload } = action;

  switch (type) {
    case 'FETCH_INIT':

  }
};